#!/usr/bin/env bash
# Deprecated: Use voice-mode-cli instead

echo "This script is deprecated. Please use: voice-mode-cli exchanges tail --format pretty" >&2
echo "Run 'voice-mode-cli exchanges tail --help' for options" >&2
echo "" >&2
echo "Forwarding to new command..." >&2
echo "" >&2

# Forward with pretty format
exec voice-mode-cli exchanges tail --format pretty "$@"